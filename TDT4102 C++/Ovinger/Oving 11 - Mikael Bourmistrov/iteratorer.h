#pragma once

#include <string>
#include <vector>
#include <iostream>
#include <set>

void replace(std::vector<std::string>& refrenceVector, std::string old, std::string replacment);
void replace_direct(std::vector<std::string>& refrenceVector, std::string old, std::string replacement);